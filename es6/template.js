const firstName = "Abc";
const lastName = "Def";
const fullName = firstName + " " + lastName + " is a good boy";
const fullName2 = `${firstName} ${lastName} ${20 + 30 + 50} is a good boy`;

const multiline = "This is\n" + " a text";

const multiline2 = `This is
        a 
                text`;

console.log(multiline);
